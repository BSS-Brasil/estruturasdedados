#include <stdio.h>

#include "node.h"

node_t * createNode(char p[25]) {
  node_t * newNode = 
}

int main(int argc, char * argv[]) {
  node_t * primeiro = NULL;
  node_t * atual = NULL;
  node_t * anterior = NULL;

  char palavra[25];

  while(1) {
    printf("Digite uma palavra: ");
    scanf("%s", palavra);
    printf("Você digitou %s\n\n", palavra);


  }

  return 0;
}